enum Modes {
    ABBREVIATED = 'abbreviated',
    CURRENCY = 'currency',
    DIGIT = 'digit',
    FRACTION = 'fraction',
    MEASUREMENT = 'measurement',
    NUMBER = 'number',
    PERCENTAGE = 'percentage',
}

export default Modes
